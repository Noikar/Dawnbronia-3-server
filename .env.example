# Copy this file to .env and fill in real values. The .env file is gitignored
# and must never be committed. docker-compose reads it automatically for
# variable substitution.

# MariaDB root password. Also used by the server (AS3_DBPASS) to connect.
# Use a strong, unique value for any non-local deployment.
MYSQL_ROOT_PASSWORD=changeme
